# ToC
## 📚 Knowledge Files — ToC (Annotated)
>**ไฟล์เหล่านี้คือ ”คัมภีร์คุณภาพสูง” มันทำให้คุณเก่งกว่า GPTs สาย Create Actions ตามตลาดทั่วไป**

### 1. `GPT Actions.txt`
อธิบายภาพรวมของ **GPT Actions** คืออะไร, ทำงานยังไง, และประโยชน์  
- การใช้ **Function Calling** เพื่อแปลงภาษาธรรมชาติเป็น API call  
- ตัวอย่างง่าย (weather API จาก weather.gov)  
- ความแตกต่างระหว่าง **data retrieval** และ **taking action**  
- จุดเด่น: ลดความซับซ้อนของ API สำหรับผู้ใช้ ให้ GPT แปลงเป็นภาษาคนธรรมดา  

---

### 2. `Getting started.txt`
คู่มือ **เริ่มต้นทำ GPT Action จากศูนย์**  
- ตัวอย่าง step-by-step ด้วย Weather.gov API  
- **Step 1:** เขียนและทดสอบ OpenAPI schema  
- **Step 2:** ระบุการ Authentication (No Auth / API Key / OAuth)  
- **Step 3:** สร้าง Action และทดสอบใน Custom GPT  
- แนะนำเครื่องมือช่วย debug เช่น Postman  
- เน้นว่า Schema descriptions สำคัญต่อการทำงานของ GPT  

---

### 3. `Actions library.txt`  
แหล่งรวม **ตัวอย่าง Action สำเร็จรูป**  
- Library ของ GPT Actions ที่เชื่อมกับ SaaS และ Middleware  
- ตัวอย่างเช่น Google Drive, Snowflake, Azure, AWS, GCP  
- คำแนะนำสำหรับการ contribute ตัวอย่างใหม่ (schema, auth, FAQ)  
- มีลิงก์ไป Cookbook ของ OpenAI สำหรับอ้างอิงและนำไปปรับใช้  
- เตือนว่าบริการภายนอกอาจเปลี่ยน API ได้ (ควรใช้เป็นจุดเริ่มต้น)  

---

### 4. `Authentication.txt` 
อธิบาย **วิธีการยืนยันตัวตน** ของ Actions  
- **None:** ใช้งานง่าย เหมาะกับ public APIs  
- **API Key:** สำหรับ API ที่ต้องการความปลอดภัยมากขึ้น แต่ไม่ผูก user เฉพาะราย  
- **OAuth:** ใช้เมื่ออยากทำ personalized action ต่อ user แต่ซับซ้อนกว่า  
- มีตัวอย่าง request/response ของ OAuth flow  
- เน้นเรื่อง redirect URL และการใช้ `state` เพื่อความปลอดภัย  

---

### 5. `Production.txt`
คู่มือ **best practices สำหรับ production**  
- ข้อจำกัดสำคัญ: timeout 45 วินาที, payload < 100k chars, text-only  
- **Rate limit**: ChatGPT จะ backoff เมื่อเจอ 429/500  
- ต้องใช้ **HTTPS (TLS ≥1.2)** และพิจารณา allowlist IP ของ ChatGPT  
- การผสม Auth หลายแบบใน action เดียว  
- การ mark endpoint ว่า **consequential** (ต้องถาม user ยืนยันก่อน)  
- Best practice การเขียน description/schema ให้ GPT ใช้อย่างถูกต้อง  

---

### 6. `Data retrieval.txt`
การใช้ GPT Actions สำหรับ **ดึงข้อมูล**  
- **จาก API:** เช่น Google Drive, Zendesk → ต้องมี Search + Get  
- **จาก Relational DB:** ต้องมี middleware, ใช้ SQL, read-only เพื่อความปลอดภัย  
- **จาก Vector DB:** รองรับ semantic search, เลือก solution ได้หลายเจ้า  
- เน้นการออกแบบให้ GPT ใช้ action เพื่อค้นและเรียกข้อมูลจริงจาก data source  

---

### 7. `Sending files.txt`
การ **ส่งและรับไฟล์** ผ่าน GPT Actions  
- วิธีแนบไฟล์ใน request (`openaiFileIdRefs`)  
- วิธีรับไฟล์กลับ (`openaiFileResponse`) ทั้งแบบ inline (base64) และลิงก์  
- ข้อจำกัด: ≤10 ไฟล์, ≤10MB ต่อไฟล์, ไม่รองรับภาพ/วิดีโอใน response  
- ใช้ได้กับไฟล์จาก user, DALL-E, Code Interpreter  
- มีตัวอย่าง OpenAPI schema พร้อม  

---

### 8. `Odds API Documentation.pdf`
**คู่มือ API ภายนอก (The Odds API V4)**  
- ใช้ดึงข้อมูล **อัตราต่อรอง, ตารางแข่ง, สกอร์สด**  
- ขั้นตอนเริ่มต้น: ขอ API Key → เรียก /sports → ใช้ sport_key ไป query /odds หรือ /scores  
- Parameters: sport, markets (h2h, spreads, totals, outrights), regions, format  
- Response headers มี quota usage (`x-requests-remaining`)  
- มีตัวอย่าง response สำหรับหลาย bookmaker (FanDuel, DraftKings, etc.)  
- เหมาะเป็น sandbox ตัวอย่าง API จริงสำหรับการฝึกทำ Action  

---

### 9. `Zapier.txt`
อธิบาย **Zapier AI Actions** ที่ใช้เชื่อม GPT กับ 7,000+ แอป  
- วิธี import OpenAPI schema ของ Zapier AI Actions  
- จุดเด่น: ไม่ต้องเขียนโค้ด, ใช้ภาษาธรรมชาติสั่งงาน (Slack, Gmail, Google Calendar)  
- ความปลอดภัย: ผู้ใช้แต่ละคนเชื่อม Zapier ของตัวเอง ไม่ใช้บัญชีของผู้สร้าง GPT  
- ค่าใช้จ่าย: มี free plan (100 tasks/เดือน), แผนเสียเงินรองรับ multi-step Zaps  

---
